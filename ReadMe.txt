Fot the backend /mycar, commands:

install bundles:

 bundle install

Create database and schema and seed:

  rail db:setup

Run Server:

  rails s 


For the frontend /mycar-frontend, commands:

 npm install

 npm start 


